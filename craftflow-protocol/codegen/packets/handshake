[C->S](Handshake) Handshake {
	id@[711+] = 0x00
	id@[710-] = 0x01
	data = {
		first: u32,
		#[feature(got_removed_later)]
		second: u32 = 0,
		#[feature(temporary)]
		third: u32 = 0,
		#[feature(temporary)]
		fourth: u16 = 0,
	}
	format@[710-] = {
		first,
		#[feature(got_removed_later)]
		second,
		#[feature(temporary)]
		third: u32,
		#[feature(temporary)]
		fourth: u16,
	}
}
